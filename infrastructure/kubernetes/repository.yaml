apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: autoted-repository
  namespace: flux-system
spec:
  interval: 30s
  ref:
    branch: main
  url: ssh://git@github.com/bernardoforcillo/autoted
  secretRef:
    name: autoted-auth
  timeout: 3m
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: autoted-source
  namespace: flux-system
spec:
  interval: 30s
  path: ./infrastructure/kubernetes
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: autoted-repository
  retryInterval: 5m
  timeout: 5m
